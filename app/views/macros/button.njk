{% macro button (
    next = "",
    value = "Continue"
) %}
    {% if next %}<input type="hidden" name="next-page" value="{{ next }}">{% endif %}
    <button type="submit" class="button">{{ value }}</button>
    {# <a class="button" href="{{next}}" role="button"> {{value}}</a> #}
{% endmacro %}
